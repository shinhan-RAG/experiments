# 검색 평가 정답 기준 명세 v1 (제안)

- 작성: 2026-08-18, 240문항 전수 감사·재채점 결과 기반 (seyoung + Claude)
- 적용 대상: noah 통합 gold (train 173 / test 67, 원문 sha `40a471d5`, coord raw)
- 지위: **제안** — 사람 승인 전까지 draft. 승인 시 `_v3` 정본으로 승격
- 근거 파일: `tos-skeleton/hybrid-enrich/out/gold_*_v3_audit_draft.jsonl` · `gold_train_v4_occexp_draft.jsonl` · `qa240_audit_verdicts.json`

## 0. 왜 이 기준이 필요한가 (한 단락)

같은 실행 로그가 채점 기준에 따라 S@5 .56~.92까지 출렁였다. 원인 세 가지가 실측됐다:
① gold 오류 15% (broken 8문항 + 오매핑 43 region),
② 위치 기반 채점 × 중복 문서(동일 조문 최대 60곳, "지급하지 않는 사유" 277회 출현) → 내용 정답을 위치 미스로 처리,
③ 필수/보조 미구분 → 제목 조각을 히트로 인정.
아래 기준은 이 세 가지를 각각 봉인한다.

## 1. 정답의 단위 — evidence region

- 원본 span을 정렬 후 **gap ≤ 200자면 하나의 region으로 병합**한다 (목록 항목·같은 조항 블록).
- 문항의 정답 구조는 `[unit1 AND unit2 ...]`, 각 unit은 `[regionA OR regionB ...]`.
  - AND: 답을 쓰려면 전부 필요한 서로 다른 근거 (예: 지급사유 + 감액 단서)
  - OR: 같은 내용의 중복 (예: 요약 지급기준표 ↔ 본문 조항)

## 2. 등급 — required / supporting

- **required**: 그 텍스트 없이는 질문에 답할 수 없는 근거. 채점은 required만 사용.
- **supporting**: 관련 있으나 보조적인 것 — 조항 **제목**, 용어해설, 배경 설명. 부지표로만 집계.
- 자동 초벌 규칙(결정론): 제목 패턴(제N조/[…]/45자 미만 비문장)만 supporting, 나머지 required.
  최종 등급은 사람 판정이 우선한다. (v2에서 "멀면 보조" 자동 강등이 직접 정답을 강등한 오판 전례 있음)

## 3. 히트 판정 — 내용 기준 + 귀속 조건부

제출 유닛(청크·element)의 문자 구간이 region의 **인정 위치** 중 하나와 겹치면 히트.

인정 위치 = 등록 위치 + **동일 문구 출현 확장**:
- region 텍스트(공백 제거 앞 160자)와 정확히 일치하는 문서 내 모든 위치를 OR 멤버로 자동 추가한다 (결정론, LLM 0회).
- 단, **질문 유형으로 범위를 가른다**:
  - **특약 지정 질문** (질문에 특약명 명시 — 예: "재해장해특약에서…"): 해당 특약 scope 안의 출현만 인정. 다른 특약의 동일 문구는 오답.
  - **일반 질문** (특약 미지정 — 예: "같은 날 여러 수술 받으면…"): 문서 전체 출현 인정.
- 재조판·부분 일치(표로 재구성된 요약 등)는 자동 인정하지 않는다. 사람이 OR 멤버로 명시 등록한 것만 인정.
  (3-gram 유사도류 퍼지 매칭은 상용구 오인정 위험 때문에 채점에 사용 금지 — 진단용으로만)

## 4. 지표

| 지표 | 정의 | 역할 |
|---|---|---|
| **sufficient@10** | 모든 required unit이 top-10 안에서 회수됨 (OR은 1개면 충족) | **주지표** — "이 결과로 답을 쓸 수 있는가" |
| S@5 | 첫 required 히트가 top-5 안 | 부지표 (기존 결과와 연속성) |
| required 회수율 | 회수한 unit 수 / 전체 unit 수 | 진단용 |
| supporting 커버리지 | 보조 region 회수율 | 진단용 |

- top-k는 10으로 고정 (하네스 제출 상한). R@20 이상을 보려면 하네스 수정이 선행돼야 한다.
- 서로 다른 gold 버전·모집단의 수치를 한 표에 놓지 않는다 (기존 등록 규약 준용).

## 5. 문항 자격

- **broken** (정답 부재/오매핑 과반, 전수 감사 8문항): 채점 모집단에서 **제외**하고 수리 대장에 등재. 수리 완료 후 복귀.
- not_answerable 판정 문항은 애초에 분리 (기존 관행 유지).
- 모든 결과 보고에 "모집단 n = 총 문항 − broken − not_answerable"을 명기.

## 6. gold 관리 규율

1. **LLM 판정은 초안**이다. gold 반영은 사람 승인 후에만. (LLM이 만든 라벨을 LLM으로 평가하는 순환 금지)
2. 수정은 버전 태그로만 (v3 정본 → v3.1 …). 결과 JSON에 gold 버전·sha 병기 (기존 규약 준용).
3. 오류 발견 루프: 검색기 실패 문항만 열어 **검색실패 / gold오류 / 정의애매** 3분류 → gold오류는 수리, 애매는 본 명세 개정으로.
4. test는 봉인 유지. 본 명세 확정 후 1회 개봉해 재채점하는 것을 개봉 대장에 기록.

## 7. 판정 의사코드

```
hit(unit, region):
    positions = [등록 span] + exact_occurrences(normalize(region.text)[:160])
    if 질문.특약지정: positions = [p for p in positions if scope(p) == 질문.특약]
    return any(overlap(unit.char_range, p) for p in positions)

score(문항):
    units = group_by_or(required_regions(문항))
    sufficient = all(any(hit(u, r) for u in top10 for r in unit) for unit in units)
```

파라미터 고정값: 병합 gap 200 / 출현 확장 정확일치 160자(공백 제거) / top-k 10.

## 8. 이 기준으로 잰 현재 성적 (8/14 로그, broken 제외, 특약 귀속 조건 미적용 상태)

| arm | S@5 | sufficient@10 |
|---|---:|---:|
| BASE | .703 | .685 |
| V9 | .729 | .687 |
| BASE_CLM | **.760** | **.713** |
| V9_CLM | .741 | .711 |

※ 특약 지정 질문의 귀속 조건(§3)을 적용하면 이보다 낮아진다 — 적용 스크립트는 후속 작업.

## 9. 확정까지 남은 사람 결정 3가지

1. broken 8 + gold_error 28문항 수리 승인 (`qa240_audit_verdicts.json` 검수, 1~2시간)
2. §3 질문 유형 분류(특약 지정/일반)의 판정 방식 — 규칙(특약명 문자열 존재)으로 충분한지
3. supporting 등급 표본 20건 무작위 대조 (LLM 초안 신뢰도 확인)
