# Part 0.6 R1 — 실행 결과 기록 (2026-08-05)

방법·입력·공식·관측값·제약만 기록한다. 설계·사전 등록: PART0_6_NEXT_DESIGN_20260805.

## 입력 identity

- test = Part 0 동결 QA 500 (byte 재사용) · dev = Part 0.5 dev QA 300 (재사용)
- 튜닝(dev 전용) 후 동결: tuned_params_r1 SHA `eb65263bbecf…`
  - T1 BM25F: w_base=1.0 · w_idx=4.0(전 라운드 승계) · **w_fm=0.25 · b_fm=0.9 · b_idx=0.75** (12조합 그리드)
  - T2 verify: **depth=20** (dev mixed MRR .050 > depth50 .039 / depth100 .041)
- 결과 발행: `part06r1_2d784048a4b0459a` · 브랜치 `feat/part0-6-r1`

## 조건과 결과 (test 500)

| 조건 | Hit@1 | Hit@5 | MRR@10 | vs C1 ΔMRR [95% CI] | identity ΔH1 [CI] | 판정 |
|---|---|---|---|---|---|---|
| C1 Index (기준) | .360 | .408 | .382 | — | — | — |
| C2b BM25F 2차 | .378 | .438 | .401 | +0.018 [+0.002, +0.035] | [−0.040, +0.030] | ✗ 기각(identity CI 마진 −0.02 미달) |
| C4 라우터(1R 재현) | .370 | .436 | .397 | +0.015 [+0.004, +0.027] | ±0 [0, 0] | ✅ 채택 재확인 |
| C4b verify 라우터 | .366 | .430 | .395 | +0.012 [+0.002, +0.023] | ±0 [0, 0] | ✅ 기준 통과 |

스위트별 (Hit@1/Hit@5):

| 조건 | identity | content | mixed |
|---|---|---|---|
| C1 | .880/.970 | .010/.015 | .020/.070 |
| C2b | .875/.965 | .040/.065 | **.060/.130** |
| C4 | .880/.970 | .030/.075 | .030/.090 |
| C4b | .880/.970 | .030/.075 | .010/.060 |

## C4b vs C4 직접 대결 (paired)

| 지표 | Δ [95% CI] | W/T/L |
|---|---|---|
| MRR@10 | −0.0027 [−0.0078, +0.0007] | 4/491/5 |
| Hit@5 | −0.0060 [−0.0140, +0.0000] | 0/497/3 |
| mixed MRR@10 | −0.0134 [−0.0391, +0.0036] | — |

관측: verify 분할(fm 멤버십 필터, depth 20)은 cascade-K20 mixed 경로 대비 우위를 보이지
못했고 방향은 음(mixed h5 .060 vs .090), CI는 0을 포함. dev(depth=20 유리)와 test의
방향 불일치 — mixed n=100 소표본 변동 범위 내. **T2 가설(verify가 mixed 개선)은 이번
구현·표본에서 미확인.**

## 구조적 관측 2건

1. **BM25F×비열등 검정의 통계적 한계**: 2라운드 연속 동일 사유 기각. identity 점추정
   손실 −0.005 수준에서 n=200의 CI 폭(±0.035)이 마진(−0.02)을 구조적으로 초과 —
   현 검정 설계에서 BM25F 계열은 사실상 통과 불가. 통과하려면 identity 표본 확대 또는
   마진 재설정의 새 사전 등록이 필요(사후 완화하지 않음). 라우터 계열은 identity 경로를
   물리적으로 우회하므로 Δ가 항등적으로 0 — 이 기준 하에서 유일하게 안전한 결합 형태.
2. **T6 오류 분석**(별도 파일 t6_error_analysis.json): C1 identity 미스 24건 =
   동일 상품 내 버전·종류 혼동 20건(그중 2~5위 근소 미스 18건) + 타 상품 4건.
   질문 유형별 최신 9 · 연도 9 · 종류 6. 차기 후보 관측: identity 경로에
   연도/최신 정확 키(exact key) verify 필터.

## 챔피언

변동 없음 — **C4 라우터 (.397 MRR)**, 두 독립 실행(Part 0.5 · R1)에서 동일 수치 재현.

## 제약

1. C4b의 "기준 통과"는 C1 대비이며, 현 챔피언 C4 대비 개선이 아님 — 채택 갱신 없음.
2. mixed 스위트 n=100 — dev/test 방향 반전이 발생하는 표본 규모임을 확인.
3. verify 구현은 fm 부분 문자열 멤버십(ANY 토큰) 단일 변형만 검증 — 임계값/AND
   의미론/exact_keys 변형은 미탐색.
4. latency는 사전 계산 순위 재사용 구조로 조건 간 비교 불가.
