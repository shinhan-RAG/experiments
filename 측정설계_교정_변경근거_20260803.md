# Part 1~4 측정 설계 교정 — 변경 근거·계산식·대안 비교 (2026-08-03)

> 대상: `dr-dci/` 실험 코드. `docs/0803/증강실험_디버깅_QA_정리_20260803.md`의 Q3에서
> 지적된 측정 설계 문제 5가지에 대한 코드 수정의 **왜/어떻게/무엇을 포기했나** 기록.
> 이 문서만 읽어도 "왜 이렇게 고쳤는지"를 재구성할 수 있게 쓰는 것이 목적이다.

## 용어 사전 (먼저 읽을 것)

| 용어 | 뜻 |
|---|---|
| **arm** | 실험 조건 하나 (예: baseline, taxonomy_only). 팔 비교하듯 조건끼리 비교해서 arm |
| **pull** | 에이전트가 임베딩 검색으로 문서를 워크스페이스에 가져오는 단계 |
| **probe** | LLM 없이 원 질의로 검색만 1회 실행해 순위 지표를 재는 진단 실행 |
| **Gold R@W** | 최종 워크스페이스에 정답 문서가 살아남은 비율 (기존 주 지표) |
| **paired 비교** | 같은 질의에 대한 두 조건의 값 차이를 질의별로 묶어 비교하는 것. 질의 난이도 차이가 상쇄되어 검정력이 높아진다 |
| **bootstrap CI** | 데이터를 복원추출로 재표집해 평균의 95% 신뢰구간을 구하는 방법 |
| **p-value** | "효과가 없다"는 가정 아래 지금 관측된 차이만큼 극단적인 값이 우연히 나올 확률 |
| **sign-flip permutation** | 질의별 델타의 부호를 무작위로 뒤집어 귀무분포를 만드는 p-value 계산법 |
| **replicate** | 같은 실험의 반복 실행 1회분 |
| **confound(교란)** | 비교하려는 변수 외에 함께 달라져버린 요소 (예: 리랭커가 한쪽에만 붙음) |

## 배경 — 무엇이 문제였나

기존 Part 1~4는 결과 자체는 나오지만, **개별 arm의 ±10% 등락을 판정할 수 없는 구조**였다:

1. Gold R@W 하나로 "검색이 나빠졌는지 / 에이전트가 이상했는지"를 뭉뚱그려 측정
2. 상대 %만 보고돼 바닥 위의 +29% 같은 착시 발생
3. n=50·단일 실행·CI만 있고 p-value 없음 → 검정력 부족
4. 리랭커가 Hybrid에만 부착, 질의-정답 어휘 겹침 → 비교가 기울어짐
5. boost +0.15가 튜닝 없는 고정값 → "taxonomy가 해롭다 vs +0.15가 과했다" 분리 불가

핵심 발견은 **필요한 부품 대부분이 이미 코드에 있었고 배선만 안 돼 있었다**는 것:
`run_pull_probe()`(probe), `paired_bootstrap_test()`(permutation 검정),
`RetrieverConfig.taxonomy_bonus`(보너스 필드), `PullRetriever._rerank()`(리랭커)가
모두 구현돼 있었으나 Part 1~4 경로에 연결되지 않았다. 따라서 이번 수정의 원칙은
"새 알고리즘 도입"이 아니라 **기존 부품의 배선 + 계측 추가**이며, 모든 변경은
additive(새 키 추가만, 기본 동작 불변)로 해서 과거 결과 JSON과 호환된다.

---

## 수정 1 (C) — 비교 분석에 절대값·상대%·p-value 병기

**파일**: `dr-dci/src/eval/comparison.py` `paired_bootstrap_delta()`

### 왜

기존 반환은 `mean_delta`와 bootstrap CI뿐이었다. 문제 두 가지:

- **절대값 부재**: "stack_all +29%"의 실체가 0.039→0.050(질의 50개 중 0.5개 차이)임을
  결과 파일만 보고 알 수 없었다. 이제 `control_mean`/`treatment_mean`이 항상 함께
  기록되므로 상대 %가 단독으로 돌아다닐 수 없다.
- **p-value 부재**: CI가 0을 포함하는지로만 판단했는데, CI는 "효과 크기의 불확실성"이지
  "우연일 확률"이 아니다. 다중 arm 비교(8-arm)에서 판단 기준이 필요했다.

### 계산식과 그 이유

```
mean_delta   = mean(treatment_i - control_i)          # 질의 i별 paired 델타
relative_pct = mean_delta / control_mean * 100        # control_mean≈0이면 None
CI95         = percentile bootstrap (복원추출 10,000회, 2.5/97.5 분위)
p_value      = sign-flip permutation:
               부호를 무작위로 뒤집은 |평균|이 관측 |평균| 이상인 비율
               p = (extreme + 1) / (iterations + 1)
```

- **paired(질의별 델타) 기반인 이유**: 질의마다 난이도가 다르다. 조건 A·B의 전체 평균을
  따로 비교하면 질의 난이도 분산이 노이즈로 들어가지만, 같은 질의의 차이만 보면 난이도가
  상쇄된다. n=50의 부족한 검정력을 구조로 보완하는 표준 기법.
- **p-value를 bootstrap이 아니라 sign-flip으로 구한 이유**: bootstrap 분포는 "관측된
  효과 주변의 불확실성"이지 귀무가설(효과 0) 분포가 아니다. 귀무가설을 직접 시뮬레이션
  하는 permutation이 정공법이고, paired 설계에서는 sign-flip이 그 표준형이다.
  기존 `src/eval/metrics.py:paired_bootstrap_test`가 이미 같은 정의를 쓰고 있어
  정의 일관성도 유지된다.
- **`(extreme+1)/(n+1)` 보정**: p=0이 나오는 것을 막는 표준 보정(관측값 자신을 극단
  집합에 포함). 유한 표본 permutation에서 p=0은 논리적으로 불가능하다.
- **`relative_pct`를 control_mean≈0에서 None으로**: 0.001→0.01이 "+900%"로 기록되는
  것을 원천 차단. None은 "상대 %가 무의미한 구간"이라는 명시적 신호다.

### 대안 비교

| 대안 | 장점 | 왜 안 썼나 |
|---|---|---|
| `metrics.paired_bootstrap_test`를 import해 재사용 | 코드 중복 없음 | 순수 파이썬 `random` 루프라 느림(analysis 블록이 수십 개면 체감). comparison.py는 이미 numpy 기반이라 벡터화 구현이 자연스럽고, 정의는 동일하게 맞췄다 |
| McNemar 검정 (질의별 hit/miss 이진화) | 이진 지표에 정확 | recall이 0/1이 아닌 연속값인 arm(gold 다수 질의)에서 정보 손실. permutation은 연속·이진 모두 커버 |
| t-검정 | 빠르고 익숙함 | n=50에 0/1 뭉침이 많은 분포라 정규성 가정이 약하다. permutation은 분포 무가정 |
| Bonferroni 등 다중비교 보정 자동 적용 | 8-arm 다중비교 엄밀화 | 어떤 비교가 "가족"인지는 분석 시점 판단이라 러너가 강제하면 오히려 경직. p-value 원값을 저장하고 보정은 보고서 단계에서 하도록 남김 |

**단점(한계)**: p-value가 있어도 n=50의 검출 한계 자체는 그대로다 — 작은 효과는 여전히
안 보인다(그건 수정 7의 replicate와 질의 확충이 다룰 문제). 또 iterations=10,000의
sign-flip이 추가되어 analysis 계산이 약간 느려진다(numpy 벡터화로 블록당 수십 ms 수준).

---

## 수정 2 (A) — Part 1/3/4에 retrieval-only probe 병행 배선

**파일**: `dr-dci/run_experiment.py` `run_part1`/`run_part3`/`run_part4`, `--probe-only` 확장

### 왜

Gold R@W는 "pull → 워크스페이스 적재 → 에이전트 행동"을 전부 거친 뒤의 합산 지표다.
taxonomy/prefix는 pull(검색)에 개입하고 tags/metadata는 에이전트 필터에 개입하는데,
합산 지표로는 **어느 단계가 망가졌는지 구분할 수 없다**. Part 2 probe와 Part 5는 이미
이 반성으로 probe를 붙였는데 정작 원조 실험인 Part 1과 3/4에는 없었다.

### 어떻게 (설계 원칙)

- **같은 retriever 인스턴스 공유**: probe용 retriever를 따로 만들면 임베딩 캐시·구성이
  달라져 "probe와 agent가 다른 검색기를 본" 새 confound가 생긴다. Part 5의 기존 패턴
  (러너에서 build → probe → 같은 인스턴스를 `cached_retriever`로 agent에 전달)을
  그대로 재사용했다.
- **Part 1 probe의 해석**: probe는 원 질의를 taxonomy_filter 없이 1회 pull한다.
  Part 1의 taxonomy는 에이전트가 스스로 filter를 골라야 발동하므로, **taxonomy arm의
  probe가 baseline probe와 같게 나오는 것 자체가 "차이는 에이전트 행동"이라는 분리
  증거**다. prefix arm은 임베딩이 달라 probe에도 차이가 드러난다 — 4종 증강 중 "검색단을
  실제로 바꾸는 것"과 "에이전트단만 바꾸는 것"이 결과 파일에서 처음으로 구분된다.
- **Part 3**: 세 approach가 같은 인덱스(prefix·taxonomy 동일)를 쓰므로 probe는 arm 간
  동일해야 정상이다. 다르면 배선 버그다 — probe가 자가검증 장치를 겸한다.
- **Part 4의 hybrid**: HybridRAG는 파이프라인이라 pull probe를 직접 못 돌린다. 대신
  파이프라인이 반환한 `retrieved_docs`에 같은 rank 지표를 사후 채점해 검색단끼리 비교
  가능하게 했다. 단 latency는 경로가 달라 비교 무효(manifest에 명시).
- **`--probe-only` 확장**: 임베딩 서버만 있으면 LLM/judge 없이 배선을 검증할 수 있다.
  실험 비용(OpenAI API)을 쓰기 전에 파이프라인을 확인하는 저비용 경로.
- **Part 3/4 manifest·analysis 신설**: 기존에는 `save_results("part3_tags", all_results)`
  한 줄로 manifest도 analysis도 없었다. git_commit·seed·arm 구성이 기록되지 않으면
  결과 파일이 "어느 코드로 왜 돌았는지" 증명 불가라, Part 1 수준으로 채웠다.

### 대안 비교

| 대안 | 장점 | 왜 안 썼나 |
|---|---|---|
| 에이전트의 첫 pull 결과로 검색단을 측정 | 별도 probe 실행 불필요 | 에이전트가 질의를 재작성하므로 "검색기 품질"과 "재작성 품질"이 다시 섞인다. probe는 원 질의 고정이 핵심 |
| probe를 별도 스크립트로 분리 실행 | 러너 무변경 | retriever를 다시 빌드하게 되어 인스턴스 공유가 깨지고, 실행 시점 차이(캐시·엔드포인트)가 새 변인이 된다 |
| 전 arm에 rank_all()로 전량 순위 저장 | 사후 분석 자유도 최대 | 결과 파일이 코퍼스 크기에 비례해 커진다. top-20 저장(`ranked_top20`)으로 충분 — dedup_rescore.py도 이 필드로 재채점한다 |

**단점**: arm당 probe 50회 pull이 추가된다(임베딩 캐시 히트 시 질의 임베딩 50회 수준,
수 초). 결과 JSON에 arm당 `probe_rows` 50행이 추가돼 파일이 커진다(질의 50 규모에선
수백 KB). Part 4 hybrid의 rank 지표는 rerank_top_k=20개 안에서만 계산되므로 recall@20
초과 구간은 관측 불가라는 상한이 있다.

---

## 수정 3 (E) — taxonomy_bonus 배선 + `--sweep-bonus`

**파일**: `run_experiment.py` `build_pull_retriever`/`run_part5`, `config/*.yaml`, CLI

### 왜

+0.15는 튜닝 없이 정해진 값이었다. "taxonomy 증강이 해롭다"는 결론에는 항상
"+0.15가 과했을 뿐"이라는 반론이 가능했는데, `RetrieverConfig.taxonomy_bonus` 필드가
있는데도 `build_pull_retriever`가 전달하지 않아 **코드 수정 없이는 다른 값을 시험할 수
없었다**. 이제 yaml(`agent.taxonomy_bonus`)·CLI(`--taxonomy-bonus`)로 바꿀 수 있고,
`--sweep-bonus 0.0,0.05,0.15,0.3`으로 probe 스윕이 된다.

### 계산 방식이 이렇게 된 이유

- **bonus는 쿼리 시점 덧셈**(`sims[i] += bonus`)이므로 재인덱싱이 필요 없다. 스윕은
  retriever 하나를 빌드해 두고 `config.taxonomy_bonus` 값만 바꿔 probe를 반복한다 —
  임베딩 비용 0으로 스윕 전 구간을 잰다.
- **bonus=0.0을 스윕에 포함**: filter가 있어도 가산이 0이면 순수 dense와 동치다.
  스윕 결과에서 b0 arm이 dense와 일치하는지가 배선 자가검증이 된다(analysis에
  `probe_dense_vs_taxonomy_boosted_b0` paired 비교 자동 생성).
- **덧셈(곱셈 아님) 유지**: 기존 주석·테스트대로 곱셈 boost는 음수 cosine에서 penalty로
  반전되는 버그가 있어 덧셈을 유지한다.
- **스윕은 `--probe-only` 전용으로 강제**: agent 루프까지 bonus별로 돌리면 LLM 비용이
  스윕 크기만큼 배가되고, bonus의 효과는 검색단에서 먼저 판정하는 게 맞다(검색단에서
  전 구간 해로우면 agent 실험은 불필요).

### 대안 비교

| 대안 | 장점 | 왜 안 썼나 |
|---|---|---|
| bonus를 아예 하이퍼파라미터 최적화(grid+validation split) | "최적 bonus" 산출 | 목적이 다르다 — 지금 필요한 건 "어떤 bonus에서도 이기는 구간이 있는가"라는 민감도 곡선이지 튜닝값이 아니다. n=50에서 튜닝하면 과적합 |
| 곱셈 boost로 전환 후 score 정규화 | 스케일 불변 | cosine을 [0,1]로 재정규화하는 추가 변환이 들어가 기존 결과와 비교 불가. 음수 구간 버그 방어 테스트도 무효화됨 |
| rank 융합(RRF)로 카테고리 신호 주입 | score 스케일 문제 원천 회피 | 새 백엔드를 하나 더 만드는 일 — 이미 routed/partitioned가 "비-score 개입"을 커버한다 |

**단점**: 스윕은 taxonomy_boosted(soft boost 경로)에만 의미가 있다. hard routing
(routed/partitioned)은 bonus를 안 쓰므로 스윕 대상이 아니다. 또 `retriever.config`
필드를 루프에서 직접 바꾸는 방식은 병렬 실행과는 안 맞는다(현재 probe는 순차라 문제없음).

---

## 수정 4 (F) — 리랭커 패리티 (`pull_reranker`)

**파일**: `run_experiment.py` `build_pull_retriever`, `config/*.yaml`, CLI `--pull-reranker`

### 왜

Part 2·4의 "DR-DCI vs Hybrid" 비교에서 리랭커가 Hybrid에만 붙어 있었다(Part 4 confound,
Q3 표의 #4). 백엔드 차이를 재려는 비교에 "리랭커 유무"가 함께 실려, Hybrid 우위가
백엔드 덕인지 리랭커 덕인지 분리 불가였다. `PullRetriever._rerank()`와 gather_k 확장
로직은 이미 구현돼 있었고 `build_pull_retriever`가 reranker_url을 안 넘겼을 뿐이다.

### 설계 판단

- **기본 off**: 켜는 순간 과거의 모든 DR-DCI 결과와 비교 불가가 되므로, 기본값은 기존
  동작 그대로 두고 패리티 실험에서만 명시적으로 켠다.
- **manifest에 `pull_reranker` 상시 기록**: 플래그 기록이 없으면 리랭커 on/off run이
  결과 파일에서 구분 안 된다 — 기록이 이 수정의 완료 조건이다.
- **`allow_reranker_fallback=False` 유지**: 리랭커 서버 장애 시 조용히 원 순서로
  계속하면 "리랭커 arm"이라는 라벨이 거짓이 된다. 명시적 실패가 맞다.
- **반대 방향(hybrid에서 리랭커 떼기)은 이번 범위 외**: 패리티는 한쪽만 맞추면 성립한다.
  hybrid 쪽을 떼는 옵션은 HybridRAG 생성자 시그니처 변경이 필요해 다음 단계로 미뤘다
  (manifest에 `hybrid_reranker: true`를 명시해 비대칭이 기록되게는 해뒀다).

### 대안 비교

| 대안 | 장점 | 왜 안 썼나 |
|---|---|---|
| 양쪽 다 리랭커 제거 | 가장 단순한 패리티 | "리랭커 포함 최선의 hybrid"와의 비교라는 기존 실험 의미가 사라진다. on/off 양쪽을 선택 가능하게 하는 게 상위 호환 |
| 리랭커를 항상 켠 새 기본값 | 실무 구성과 유사 | 과거 결과 전체와 단절 + 리랭커 서버(:8002) 의존이 상시화되어 로컬 재현성 하락 |

**단점**: pull마다 gather_k = k×4 후보를 리랭커에 보내므로 켰을 때 pull 레이턴시가
눈에 띄게 는다. 리랭커 서버가 없으면 arm이 즉시 실패한다(의도된 동작이지만 원격 실행
스크립트에 서버 기동 선행 조건이 하나 늘었다).

---

## 수정 5 (G) — 패러프레이즈를 별도 파일 + `--query-variant`

**파일**: `scripts/paraphrase_queries.py`, `run_experiment.py` `load_queries`, CLI

### 왜

기존 스크립트는 `queries.jsonl`을 **덮어썼다**. 문제 세 가지:

1. 결과 JSON에 어느 질의본(원본/패러프레이즈)이 쓰였는지 기록이 남지 않는다 —
   같은 dataset 이름의 두 결과가 사실은 다른 질의로 측정된 것일 수 있다.
2. 원질의·패러프레이즈 두 세트의 paired 비교(질의 강건성 축)가 파일 스왑 없이는 불가.
3. 스왑을 잊으면 이후 모든 실험이 조용히 패러프레이즈 위에서 돈다.

### 설계

- 출력을 `queries_paraphrase.jsonl`(층화 표본이 있으면
  `agent_queries_50_paraphrase.jsonl`)로 분리. 원 파일은 불변.
- `load_queries(dataset, variant=...)`: variant 지정 시 해당 파일이 없으면
  **FileNotFoundError로 즉시 실패**. 조용한 원본 폴백을 금지한 이유는 폴백하면 결과
  라벨(`query_variant`)과 실제 질의가 어긋나기 때문이다 — `load_augmentations`의
  "요청 산출물 누락 즉시 실패" 원칙과 같은 철학.
- manifest에 `query_variant` 상시 기록.
- 구버전이 이미 덮어쓴 데이터셋 대비: `.orig` 백업이 있으면 그걸 원본으로 간주해
  variant 파일을 생성하고, `--restore`로 원복 가능. **로컬 저장소에는 `.orig`가 없음을
  확인했다** — 패러프레이즈 실행분은 서버(KT Cloud)에만 있었으므로, 서버 쪽 데이터를
  쓸 때는 `queries.jsonl`이 원본인지 먼저 확인해야 한다(잔여 리스크).

### 대안 비교

| 대안 | 장점 | 왜 안 썼나 |
|---|---|---|
| 덮어쓰기 유지 + manifest에 질의 해시 기록 | 코드 변경 최소 | 해시로 "다르다"는 알 수 있어도 "무엇이었는지"는 복원 불가. 병행 비교도 여전히 파일 스왑 필요 |
| 데이터셋 디렉토리 복제(paper-mixed-para/) | 러너 무변경 | 코퍼스·qrels·서브셋·taxonomy까지 통째로 중복(110k 코퍼스면 수백 MB). 질의만 다른데 전체를 복제하는 건 낭비이자 drift 위험 |
| 질의 파일에 원본·변형을 모두 넣고 필드로 구분 | 파일 하나 | BEIR 형식(`_id`,`text`)과 어긋나 기존 로더·외부 도구와 호환 깨짐 |

**단점**: 서버에 이미 덮어써진 데이터셋은 이 수정으로 자동 복구되지 않는다(수동 `--restore`
필요). variant 파일명 규약(`queries_<variant>.jsonl`)을 아는 사람만 새 variant를 만들 수
있다 — 규약은 스크립트 docstring과 이 문서에 기록했다.

---

## 수정 6 (B) — 단계 분리 funnel: doc_id 계측 + `analyze_stage_funnel.py`

**파일**: `dr-dci/src/agent/dci_agent.py`, 신규 `dr-dci/scripts/analyze_stage_funnel.py`

### 왜

probe(수정 2)는 "원 질의 기준 검색단"을 분리하지만, **에이전트가 실제로 무엇을
가져오고 무엇을 봤는지**는 여전히 최종 상태(Gold R@W)로만 남았다. 기존 trace는 grep이
매치 "개수", find가 "matched 수"만 기록해 **gold가 필터 결과에 노출됐는지 사후 복원이
불가능**했다(이번 조사에서 확인한 계측 공백). 그래서:

- `pull_stats[*].result_doc_ids`: pull이 가져온 문서 ID (pull당 최대 top_k개)
- trace의 grep `match_doc_ids` / find `doc_ids`: 필터 결과에 보인 문서 ID (상한 20)

를 추가하고, 결과 JSON에서 4단계 funnel을 복원하는 사후 스크립트를 만들었다.

### funnel 계산식

```
① pull_recall            = |(∪ pull별 result_doc_ids → parent 사상) ∩ gold| / |gold|
② filter_gold_exposure   = (grep/find 결과에 gold가 1회 이상 보인 질의 수)
                           / (grep/find를 쓴 질의 수)
③ read_recall            = 기존 필드 (read한 문서 ∩ gold)
④ final gold_recall      = 기존 지표 (Gold R@W)
```

- **②를 "제거율"이 아니라 "노출율"로 정의한 이유**: 이 저장소의 grep/find는 워크스페이스
  문서를 **제거하지 않는다**(조회 전용). 따라서 "필터가 gold를 걸러냈다"는 서술은 구조적으로
  성립하지 않고, 성립하는 질문은 "에이전트가 본 필터 결과에 gold가 있었는가 = 에이전트의
  주의에서 gold가 탈락했는가"다. 스크립트 출력에도 이 해석 주의를 상시 인쇄한다.
- **①은 probe와 다른 축**: probe는 원 질의 고정, ①은 에이전트의 재작성 질의 기준.
  둘을 비교하면 "에이전트의 질의 재작성이 검색을 돕는가"까지 분리된다.
- **구버전 JSON 호환**: 신규 키가 없으면 ①②를 `n/a (pre-instrumentation)`로 표기하고
  ③④만 산출 — 재실험 없이 과거 결과에도 실행 가능(합성 데이터로 신·구 스키마 모두 검증함).

### 대안 비교

| 대안 | 장점 | 왜 안 썼나 |
|---|---|---|
| 순수 사후 스크립트(계측 무추가) | 러너 무변경 | 불가능 — 기존 trace에 doc_id가 없어 ①②를 복원할 수 없다 (이번 조사로 확정) |
| 러너에서 funnel을 실시간 계산해 결과에 저장 | 스크립트 불필요 | funnel 정의가 바뀔 때마다 재실험이 필요해진다. 원시 doc_id를 남기고 해석은 사후로 미루는 쪽이 재분석 자유도가 높다 |
| 전체 tool 결과 원문(trace 전문) 저장 | 정보 최대 | grep 매치 텍스트까지 저장하면 결과 파일이 수십 배로 는다. doc_id 리스트(상한 20)가 funnel에 필요충분 |

**단점**: 질의당 결과 행이 커진다(pull 수 × top_k개 ID 문자열 — 질의 50 규모에선 무해,
aihub-full 6천 질의 경로는 파일 크기 확인 필요). ②는 "필터를 쓴 질의"가 분모라 필터를
아예 안 쓴 arm에서는 n/a다(이는 결함이 아니라 "에이전트가 필터를 안 썼다"는 정보다 —
Q7의 `taxonomy_filtered_pulls` 카운터와 같은 계열).

---

## 수정 7 (D) — `--seeds` replicate 반복 + `aggregate_seeds.py`

**파일**: `run_experiment.py` `main()`, 신규 `dr-dci/scripts/aggregate_seeds.py`

### 왜

run 간 변동이 ±0.06으로 실측됐는데 arm당 1회 실행이라 "이번 run이 운 좋은 쪽이었는지"를
알 수 없었다. `--seeds 42,43,44`로 전체를 반복 실행하고, 집계 스크립트가 arm별
mean/std/min/max와 **paired delta의 부호 일관성**(3회 모두 같은 방향인가)을 보고한다.

### 정직성 주의 — 이것은 "seed 스윕"이 아니라 "replicate"다

agent LLM은 temperature=0이고, seed가 실제로 닿는 곳은 bootstrap RNG와 Part 4 질의
샘플링뿐이다. 즉 **run 간 분산의 주원인은 seed가 아니라 OpenAI API의 비결정성(모델
스냅샷)과 임베딩 서버의 수치 비결정성**이다. 그래서:

- 명칭을 replicate로 쓰고 manifest에 `seed`와 `replicate_index`를 분리 기록
- 집계 스크립트가 run별 `system_fingerprints` 합집합을 병기 — 분산이 API 스냅샷 차이와
  겹치는지 판별 근거를 남긴다
- **probe-only replicate는 std=0이어야 정상**이라는 성질을 자가검증으로 쓴다
  (0이 아니면 캐시/엔드포인트 배선이 run 간 달랐다는 뜻)

### 대안 비교

| 대안 | 장점 | 왜 안 썼나 |
|---|---|---|
| temperature>0 + seed로 LLM 분산 직접 생성 | "LLM 비결정성"을 명시적으로 측정 | 실험의 기존 통제(온도 0)를 깨고 새 변인을 도입한다. 지금 필요한 건 현 조건의 재현 분산이다 |
| 질의를 500개로 늘리기 | 검정력의 근본 해결 | 코드가 아니라 데이터 작업(gold 라벨링)이라 이번 범위 밖. 코드는 이미 질의 수에 무관하게 동작한다 — 향후 질의 확충 시 그대로 적용됨 |
| 러너가 replicate를 자동 3회 강제 | 잊어버림 방지 | LLM/judge 비용이 무조건 3배가 된다. 비용이 큰 실험이라 opt-in이 맞다 |

**단점**: 비용이 replicate 수에 비례한다(임베딩은 캐시라 무료, LLM/judge는 배수).
결과 파일이 replicate마다 따로 생겨 집계 스크립트 실행이 한 단계 더 필요하다.
같은 초에 저장될 때의 파일명 충돌은 save_results의 dedup 접미사로 방지했다.

---

## 파일별 변경 요약

| 파일 | 변경 | 수정 |
|---|---|---|
| `dr-dci/src/eval/comparison.py` | delta 블록에 control/treatment_mean, relative_pct, p_value(sign-flip 벡터화) | C |
| `dr-dci/run_experiment.py` | Part1/3/4 probe 배선·retriever 호이스팅, Part3/4 manifest/analysis 신설, `--probe-only` 확장, build_pull_retriever에 taxonomy_bonus·pull_reranker 전달, load_queries variant, `--seeds/--taxonomy-bonus/--sweep-bonus/--pull-reranker/--query-variant` CLI, save_results 파일명 충돌 방지, manifest 확장(seed·replicate_index·query_variant·bonus·reranker·embedding_endpoint) | A,E,F,G,D |
| `dr-dci/config/experiment*.yaml` (6종) | `agent.taxonomy_bonus: 0.15`, `agent.pull_reranker: false` 명시 | E,F |
| `dr-dci/src/agent/dci_agent.py` | pull_stats에 `result_doc_ids`, trace summary에 grep `match_doc_ids`/find `doc_ids` | B |
| `dr-dci/scripts/paraphrase_queries.py` | 덮어쓰기 → `queries_paraphrase.jsonl` 별도 출력, `--restore`, `.orig` 감지 | G |
| `dr-dci/scripts/analyze_stage_funnel.py` (신규) | 4단계 funnel 사후 분석, 구버전 JSON graceful degrade | B |
| `dr-dci/scripts/aggregate_seeds.py` (신규) | replicate 집계(mean/std/부호 일관성/fingerprint) | D |
| `dr-dci/tests/test_comparison.py` (신규) | p-value·절대값·상대% 검증 5건 | C |
| `dr-dci/tests/test_stage_instrumentation.py` (신규) | doc_id 계측 + bonus 배선이 순위를 바꾸는지 3건 | B,E |
| `dr-dci/tests/test_query_variant.py` (신규) | variant 선택·폴백 금지 3건 | G |

## 검증 결과 (2026-08-03 로컬)

- `pytest tests/` — **116건 전부 통과** (기존 105 + 신규 11)
- `python run_experiment.py --help` — 정상 (cp949 콘솔 인코딩 이슈로 help 문자열의
  em-dash를 제거하는 수정 포함)
- `--part 5 --sweep-bonus 0.1` (probe-only 없이) — 데이터 로드 전에 ValueError로 즉시
  거부 확인
- `analyze_stage_funnel.py` — 합성 결과 JSON으로 신규 스키마(funnel 산출)·구버전
  스키마(`n/a (pre-instrumentation)` 강등) 모두 검증
- probe 실행이 포함된 end-to-end는 임베딩 서버(:8101)가 필요해 로컬 미실행 —
  서버에서 `python run_experiment.py --part 1 --focused --probe-only`로 확인할 것

## 서버(KT Cloud)에서 돌릴 때 권장 순서

```bash
# 1) 배선 검증 (LLM 불필요, 임베딩 서버만)
python run_experiment.py --part 1 --focused --probe-only
python run_experiment.py --part 3 --probe-only
python run_experiment.py --part 4 --probe-only

# 2) bonus 스윕 (routing 파일 필요)
python run_experiment.py --part 5 --probe-only --sweep-bonus 0.0,0.05,0.15,0.3

# 3) replicate 자가검증 (probe는 결정적 → std=0이어야 정상)
python run_experiment.py --part 1 --focused --probe-only --seeds 42,43
python scripts/aggregate_seeds.py results/part1_taxonomy_focused/*.json

# 4) 본 실험 (LLM 비용 발생) — 이후 funnel 분석
python run_experiment.py --part 1
python scripts/analyze_stage_funnel.py results/part1_stacking/<ts>.json
```

## 이 수정으로도 해결되지 않는 것 (남은 한계)

1. **n=50의 검출 한계**: paired + p-value + replicate는 판정의 정직성을 높일 뿐,
   ±5% 수준의 작은 효과를 검출하려면 질의 수백 개가 필요하다(데이터 작업).
2. **hybrid 리랭커 off 옵션 부재**: 패리티는 DR-DCI 쪽 on으로만 가능. 양쪽 다 떼는
   비교는 HybridRAG 시그니처 수정이 필요하다.
3. **서버 쪽 질의 파일 오염 가능성**: 서버에서 구버전 paraphrase를 돌린 데이터셋은
   `queries.jsonl`이 패러프레이즈본일 수 있다 — 서버에서 `.orig` 존재를 확인하고
   `--restore` 후 variant 재생성이 선행돼야 한다.
4. **metadata의 pull 개입(§3-5 미검증 과제)**: metadata를 검색 전에 개입시키는 것은
   별도 백엔드 설계(문서축 라우팅이 그 답에 가깝다)로, 이번 측정 교정의 범위 밖이다.
