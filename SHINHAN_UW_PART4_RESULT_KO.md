# 언더라이팅 문서 지식화 — 검색 품질 가능성 검토

**요청**: 언더라이팅 실무문서를 지식화(RAG)할 경우 검색 품질이 나올지 대략적으로 검토
**대상**: `0730 임시 검증/원본` 7개 문서 (xlsx 3 / pdf 3 / pptx 1)
**진행**: 설계 → 구현 → 실측까지 완료 (2026-07-30, KT Cloud A100)

---

## 결론

### 현재 방식 그대로는 부족합니다. 다만 원인이 문서 품질이 아니라 표 검색과 답변 통제에 있어, 개선 가능성은 높습니다.

| 판정 항목 | 결과 |
|---|---|
| 문서를 검색 가능한 형태로 만들 수 있는가 | **가능** — 파싱·청킹 유효 확인 |
| 지금 수준으로 업무에 붙일 수 있는가 | **아니오** — 6건 중 1건은 근거를 못 찾음 |
| 개선하면 쓸 만해지는가 | **가능성 높음** — 실패 원인이 특정됨 |
| 단순히 벡터DB에 넣는 것으로 되는가 | **아니오** — 상위 5건 적중률 48% |

**한 줄 요약**: 문서 자체는 지식화에 문제가 없다. 대형 반복 표(1,897행 등)의 검색이
병목이고, 근거를 못 찾아도 답을 만들어내는 것이 더 큰 문제다. 둘 다 대응 가능한 종류다.

---

## 실측 결과

7개 문서를 563개 조각으로 나누고, **다른 문서 2,802개 조각을 노이즈로 섞어**
총 3,365개에서 질문 50개로 측정했습니다. 노이즈를 섞은 이유는 실제 환경처럼
"비슷한 문서가 많은 상황"을 만들기 위해서입니다.

| 방식 | 상위 5건 적중 | 상위 20건 적중 | 정답률 | **근거 확보 정답률** |
|---|---|---|---|---|
| 단순 어휘 검색만 | 48% | 68% | — | — |
| 단순 벡터 검색만 | 48% | — | — | — |
| **제대로 갖춘 파이프라인** | **78%** | **84%** | 74% | **70%** |

- **상위 N건 적중** = 정답이 담긴 문서 조각이 검색 결과 상위 N개 안에 들어온 비율
- **근거 확보 정답률** = 답도 맞고 근거 문서까지 제대로 찾아온 비율 (가장 실질적인 지표)
- "제대로 갖춘 파이프라인" = 벡터 검색 + 어휘 검색 결합 + 재순위 모델

---

## 이유 — 왜 이런 결과가 나왔는가

### 1. 근거를 못 찾았는데 답을 만들어냅니다 (가장 심각)

정답 문서를 **검색하지 못한 상태에서 정답 판정이 난 건이 2건** 있습니다. 즉 모델이
문서 없이 자기 지식으로 답을 지어냈고, 그게 맞아버린 경우입니다.

이래서 정답률 74%를 그대로 믿을 수 없습니다. 근거까지 확보한 비율은 **70%**입니다.

언더라이팅 업무에서 가장 위험한 형태입니다 — **근거 없는 답이 정답처럼 보입니다.**
가입한도나 인수 가능 여부를 이렇게 답하면 사고로 이어집니다.

> 대응: 답변 시 근거 조각 인용을 강제하고, 근거를 못 찾으면 답하지 않게 막습니다.
> 구현 난이도는 낮습니다.

### 2. 검색 실패 8건이 전부 표에서 났습니다

실패한 8건 모두 표 조각이었고, 표가 아닌 조각(텍스트 7건 + 도해 1건)은 전부 성공했습니다.

문서별로 보면 더 분명합니다.

| 문서 | 상위 20건 적중 | 특징 |
|---|---|---|
| (26.07) 건강 전상품 한도·가입기준 | **70%** | 252행 대형 표 포함 |
| (26.07.01) 간편예외질환 Board | **79%** | **1,897행 단일 표** |
| (25.03.01) 메디컬UW가이드라인 | 88% | 63행 표 + 설명문 |
| (2026.07.01) 기계약&보험권 합산한도 | **100%** | 표지만 소규모 |
| 건강진단기준(안) v1.2 | 100% | 7행 표 |
| [시행] 인수 기준 안내 | 100% | 공문 |
| 중대질환 분리심사 | 100% | 도해 1장 |

**표라는 형식 자체가 문제는 아닙니다.** 기계약 합산한도도 엑셀 표인데 100%입니다.
문제는 **같은 구조의 행이 수백~수천 개 반복되는 대형 표**입니다. 행 하나하나가
서로 너무 비슷해서 검색이 정확한 행을 골라내지 못합니다.

질문이 많은 두 스프레드시트(29건, 전체의 58%)가 가장 낮다는 점이 중요합니다.
이 두 문서가 실무에서 가장 많이 조회될 문서이기도 합니다.

> 대응 방향: 대형 표는 통째로 검색하지 말고 행 단위 조회(질환명·담보명으로 직접 찾기)로
> 분리하는 것이 맞습니다. 표는 검색이 아니라 **조회** 문제에 가깝습니다.

### 3. 단순히 벡터DB에 넣는 것으로는 절반을 놓칩니다

상위 5건 기준으로 **어휘 검색만(48%)과 벡터 검색만(48%)이 완전히 같습니다.**
78%로 올라가는 것은 두 방식을 결합하고 재순위 모델을 붙였을 때입니다.

"문서를 임베딩해서 벡터DB에 넣으면 된다"는 접근으로는 상위 5건에서 절반을 놓칩니다.
**결합 검색 + 재순위가 선택이 아니라 필수**이고, 이는 구축·운영 비용에 반영해야 합니다.

### 4. 반대로, 문서 파싱과 조각화는 잘 됐습니다

검색해온 문서 안에 정답 근거 문장이 들어 있는 비율이 **91%**입니다.
즉 문서를 잘못 쪼개서 근거가 잘려나간 게 아닙니다.

**실패는 "쪼개기"가 아니라 "순위 매기기"에서 납니다.** 이건 좋은 신호입니다 —
문서를 다시 만들 필요는 없고 검색 방식만 손보면 됩니다.

파싱 단계에서 정리한 것들(참고):
- 표를 행 그룹으로 나누고 각 그룹에 머리글 반복 삽입 — 안 하면 한 조각이 11만 자가 되어
  내용의 96%가 잘려나갔습니다
- 엑셀이 셀 값을 열마다 복제한 것 정리, 중복 조각 144건 제거
- 파서가 만들어낸 잘못된 문구 제거 (한국어 공문에 영어 실험 캡션이 삽입돼 있었습니다)

---

## 실무 관점 정리

| 용도 | 지금 쓸 수 있나 | 근거 |
|---|---|---|
| 담당자 참고용 검색 (사람이 확인) | **조건부 가능** | 상위 5건에 78% 포함. 후보 여러 개 보여주는 방식이면 실용적 |
| 상담 중 즉답 지원 | **아직 아니오** | 4건 중 1건 이상 오답, 근거 없는 답 발생 |
| 인수 가능·한도 자동 판정 | **아니오** | 오답 비용이 큼. 근거 확보 70%로는 부족 |

---

## 다음 단계 제안

**바로 할 것 (효과 큼, 난이도 낮음)**
1. **근거 인용 강제** — 근거를 못 찾으면 답하지 않게 막습니다. 발견 1 대응.
2. **단일 답변 대신 근거 후보 제시** — 상위 5건 78%를 전제로 한 UI 설계.

**표 문제 대응 (핵심)**
3. **대형 표는 검색이 아니라 조회로 분리** — 질환명·담보코드로 행을 직접 찾는 경로를
   따로 만듭니다. 지금처럼 표 조각을 일반 문서와 같이 검색하면 계속 실패합니다.

**추가 검증**
4. **메타데이터·태그 효과 측정** (이미 준비 완료, 실행만 남음) — 문서 종류·주제·표 여부를
   메타데이터로 붙여 검색이 개선되는지. 개선 여지가 있다는 건 확인됐습니다
   (어휘 검색이 68%에 머물러 천장에 걸리지 않았습니다).
5. **버전 혼동 검증** — 지금은 7월 문서만 넣어서 측정했습니다. 실무 최대 위험은
   "6월 기준과 7월 기준을 섞어 답하는" 것인데 이번엔 측정 자체를 못 했습니다.
   **이전 버전 문서를 주시면 바로 측정할 수 있습니다.**
6. **실제 상담 질문으로 재측정** — 아래 한계 참조.

---

## 이 검토의 한계 (수치를 보수적으로 읽어야 하는 이유)

**측정값은 실제보다 좋게 나왔을 가능성이 높습니다.**

1. **질문을 AI가 만들었습니다.** 정답 문서를 보고 질문을 생성했기 때문에, 실제 사용자
   질문보다 쉽습니다. (문서 단어를 그대로 베끼지는 않았습니다 — 겹치는 단어 비율 26%)
2. **한 조각만 보면 답이 되는 질문만 넣었습니다.** 표 + 각주 + 다른 시트를 함께 봐야
   답이 되는 실무 질문은 아예 평가에 없습니다. 그런 질문은 더 어렵습니다.
3. **노이즈 문서가 2,802개뿐입니다.** 실제 운영 코퍼스는 훨씬 크고 그만큼 어려워집니다.
4. **질문 50개** 기준이라 세부 수치(예: 표가 텍스트보다 불리하다)는 방향만 참고하시고
   확정된 사실로 인용하지는 말아주세요. 특히 표가 아닌 조각은 8건뿐입니다.

**즉 "84%"는 잘 나온 조건에서의 상한이고, 실제 운영에서는 이보다 낮게 봐야 합니다.**

---

## 부록 — 무엇을 만들었는지

7개 문서 → 검색 가능한 코퍼스 → 평가셋 → 측정까지 전 과정을 코드로 만들었습니다.
같은 문서를 다시 넣거나 문서를 추가하면 바로 재측정됩니다.

| 산출물 | 내용 |
|---|---|
| 코퍼스 | 7문서 563조각 + 노이즈 2,802조각 |
| 평가셋 | 질문 50개, 정답 근거 문장 60개 (전부 원문 확인) |
| 측정 | 표준 RAG vs 에이전트형 검색 |
| 문서 | 정제 근거·한계 정리 |

- 브랜치: `feat/shinhan-uw-element-retrieval`
- 결과: `dr-dci/results/part4_generalization/20260730_135514.json`
- 정제 상세: `dr-dci/docs/SHINHAN_UW_DATA_CLEANING_KO.md`

재현:
```bash
python scripts/build_shinhan_uw_corpus.py                              # 코퍼스
python scripts/build_shinhan_uw_qa.py                                  # 평가셋
python run_experiment.py --part 4 --config config/experiment_shinhan_uw.yaml
PYTHONPATH=. python scripts/analyze_part4_shinhan_uw.py                # 분석
```

<details>
<summary>기술 지표 원값 (필요 시)</summary>

| 방식 | n | Recall@5 | Recall@20 | nDCG@10 | Coverage@20 | 정답률(판정분) | 정답률(전체) |
|---|---|---|---|---|---|---|---|
| BM25 단독 | 50 | 0.4800 | 0.6800 | 0.4692 | — | — | — |
| DR-DCI (증강 없음) | 50 | 0.4800 | 0.6200 | 0.4740 | 0.7669 | 0.6087 (46) | 0.5600 |
| Hybrid RAG | 50 | 0.7800 | 0.8400 | 0.7059 | 0.9129 | 0.7400 (50) | 0.7400 |

정답 순위 분포 (Hybrid): 1위 29 / 2–3위 7 / 4–5위 3 / 6–10위 3 / 미검색 8
Recall 95% 신뢰구간: DR-DCI [0.48, 0.76], Hybrid [0.72, 0.94]

**해석하면 안 되는 지표**: `density@k`·`span_f1@k`(분모가 상위 k개 총 길이라 최대 ≈0.002,
`span_metrics.py:75`), `efficiency`(= recall/검색횟수라 1회 검색 방식이 구조적으로 유리).

**교란 요인**: Hybrid만 재순위 모델을 씁니다(`run_experiment.py:562-563`). 두 방식의 격차
일부는 방법 차이가 아니라 재순위 유무입니다. 또 DR-DCI는 이번 실행에서 메타데이터가 없어
탐색 도구 4개 중 2개(`grep(tag_filter)`, `find`)가 동작하지 않았습니다 — 이 방식의 제대로 된
성능은 다음 단계(메타데이터 적용) 측정에서 나옵니다.

법률·기존 신한 실험 결과와 절대 점수를 비교하지 마세요 (평가 단위와 문서·파서·청킹이 다릅니다).
</details>
