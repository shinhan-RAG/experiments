# results — 팀 측정 결과 등록 공간

담당별 폴더에 결과 요약을 올린다. 세션 로그·대용량 산출물은 올리지 않는다.

- `filesearch/`  — Semantic Tag 파일서치 (donggyu)
- `vector_search/` — 메타데이터 검색 (noah/lsh)

파일명: `<날짜>_<arm>_<모집단>.json` (summary.json 형식 그대로 또는 지표 표).
모든 결과에 gold 파일명·채점 방식·모델·n 을 명기한다.
